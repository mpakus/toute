<div class="navbar navbar-fixed-top navbar-inverse toute-navbar">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">TOUTE</a>
    </div>
    <div class="navbar-collapse collapse">
      <ul class="nav navbar-nav toute-navbar-menu">
        <!--li class="active"><a href="#">Home</a></li>
        <li><a href="#about">About</a></li>
        <li><a href="#contact">Contact</a></li-->
        <li><%= link_to t('touts.list'), touts_path %></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= t('menu.city') %> <b class="caret"></b></a>
          <ul class="dropdown-menu">
            <% City.find_each do |city| %>
              <li><%= link_to city.name, touts_path(city: city.alias, operation: params[:operation], category: params[:category]) %></li>
            <% end %>
          </ul>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= t('menu.operations') %> <b class="caret"></b></a>
          <ul class="dropdown-menu">
            <% Operation.find_each do |oper| %>
              <li><%= link_to oper.name, touts_path(city: params[:city], operation:oper.alias, category: params[:category]) %></li>
            <% end %>
          </ul>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= t('menu.category') %> <b class="caret"></b></a>
          <ul class="dropdown-menu">
            <% Category.find_each do |category| %>
              <li><%= link_to category.name, touts_path(city: params[:city], operation: params[:operation], category: category.alias) %></li>
            <% end %>
          </ul>
        </li>
      </ul>

      <!--div class="navbar-right">
        <%= form_tag(touts_path, method: "get", class: "navbar-form") do %>
          <div class="form-group">
            <%= text_field_tag(:string, params[:string], placeholder: t('form.search'), class: "form-control" ) %>
          </div>
          <div class="form-group">
            <button class="btn btn-primary"><span class="glyphicon glyphicon-search"></span></button>
          </div>
        <% end %>
      </div-->

      <div class="navbar-right">
        <%= user_controls() %>
      </div>

    </div>
  </div>
</div>
